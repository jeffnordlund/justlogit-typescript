# Change Log 

## v1.0.1
- Added in better support for the state values.  They can be either an object or an array of objects with key/value pairs.
- Changed the console message displayed when a logging call fails to show a little more detail.
